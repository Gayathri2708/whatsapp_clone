/// Keys used with [FlutterSecureStorage]. Centralized to avoid typos
/// scattered across the data layer.
class StorageKeys {
  const StorageKeys._();

  static const String accessToken = 'auth_access_token';
  static const String refreshToken = 'auth_refresh_token';
}
